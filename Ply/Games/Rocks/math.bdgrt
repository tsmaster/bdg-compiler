
int sgn_i(int x) {
  if (x < 0) {
    return -1;
  } elif (x > 0) {
    return 1;
  } else {
    return 0;
  }
}

int abs_i(int v) {
  if (v < 0) {
    return -v;
  } else {
    return v;
  }
}

float sin_f(float radians);
float cos_f(float radians);

float atan2_f(float y, float x);

