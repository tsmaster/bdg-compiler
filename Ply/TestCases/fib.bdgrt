void print_integer(int num);
void print_line();

int fib(int x) {
  if (x < 0) {
    return 0;
  } elif (x == 0) {
    return 1;
  } elif (x == 1) {
    return 1;
  } else {
    return fib(x-1) + fib(x-2);
  }
}

int main() {
  print_integer(fib(5));
  print_line();
  return 0;
}